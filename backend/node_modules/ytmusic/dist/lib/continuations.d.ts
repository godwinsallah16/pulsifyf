export interface ContinuationData {
    nextContinuationData: {
        continuation: string;
        clickTrackingParams: string;
    };
}
export interface ContinuationsContainer {
    continuations: {
        [index: number]: ContinuationData;
    };
}
export declare function createContinuation<T, C>(cookie: string, args: {
    userID?: string;
    authUser?: number;
} | undefined, parseContents: (renderer: any) => C, baseObject: T, container?: ContinuationsContainer): (() => Promise<T>) | undefined;
