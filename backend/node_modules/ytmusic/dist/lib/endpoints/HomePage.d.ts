import HomePage from '../../models/HomePage';
/**
 * Returns First Part of HomePage
 *
 * @usage
 *
 * ```js
 *  const api = new YTMUSIC(cookie)
 *  const data = await api.getHomePage()
 * ```
 *
 * You can call `data.continue()` to get next part
 * @returns {@link HomePage}
 *
 */
export declare const getHomePage: (cookie: string, args: {
    userID?: string;
    authUser?: number;
}) => Promise<HomePage>;
/**
 * Returns Full HomePage
 *
 * @usage
 * ```js
 *  const api = new YTMUSIC(cookie)
 *  const data = await api.getFullHomePage()
 * ```
 * @returns {@link HomePage}
 *
 */
export declare const getFullHomePage: (cookie: string, args: {
    userID?: string;
    authUser?: number;
}) => Promise<HomePage>;
